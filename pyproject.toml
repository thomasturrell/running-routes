[project]
name = "running-routes"
version = "0.1.0"
description = "Running routes generator"
dependencies = [
    "osmnx",
    "shapely",
    "geopy",
    "networkx",
    "requests",
    "gpxpy",
]

[tool.pytest.ini_options]
pythonpath = ["scripts"]
addopts = "-ra -q"
testpaths = ["tests"]
